#ifndef VALIDATION_H
#define VALIDATION_H

// Function prototypes for input validation
int validateAge(int age);
int validateWeight(float weight);  // Change the return type to int
int validateActivityLevel(const char *input);
void printSnarkyResponse(int responseType);

#endif
